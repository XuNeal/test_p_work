name: T3

on:
  issue_comment:
    types: [created, edited]
  
jobs:

  build:
    if: (
          github.event.issue.pull_request &&
          contains(
            fromJson('["test", "build android", "build"]'),
            github.event.comment.body
          )
        )
    name: T3
    runs-on: macos-latest
  
    steps:
    
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        ref: ${{ steps.sha.outputs.result }}
        fetch-depth: 5
          
    - name: Run Test T3
      run: |
        echo 'github.head_ref'=${{ github.event.comment.body }}
        echo 'github.event.pull_request.head.sha'=${{ steps.sha.outputs.result }}
        cat ./script/t1.sh
        echo '------'
        ./script/t1.sh
      env:
        TT_S: ${{ secrets.TT_S }}
